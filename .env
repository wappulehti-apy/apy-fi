# Used throughout different components to use a yearly logo/textures
THEME='2023'
# The element that is shown at index page.
# Supported values: 'logo', 'video', 'diilikone' or 'wappu'
INDEX_ELEMENT='wappu'
